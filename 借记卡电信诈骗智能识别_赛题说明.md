# 借记卡电信诈骗智能识别 · 赛题说明

> 第 1–5 节为赛题原文整理，第 6 节为解读与建模建议（非原文）。

---

## 1. 赛题背景（Background）

在数字经济纵深发展的 2026 年，支付结算场景向全线上化、实时化转变，极大地提升了金融普惠效率，但也使得电信网络诈骗呈现出**全链条产业化、技术对抗升级化、跨境协同隐蔽化**的新特征，借记卡业务面临着前所未有的安全挑战。

近年来，电信网络新型违法犯罪形势严峻，传统的基于规则的风控模型已难以有效应对。如何在海量交易数据中，从"被动拦截"转向"主动防御"，精准识别潜伏在正常交易流中的异常欺诈账号，成为银行风控的核心痛点。

---

## 2. 任务说明（Task description）

本赛题考察参赛选手对**复杂数据加工**及**有监督二分类模型构建**能力。

- **任务**：使用交易流水等数据，尝试衍生新的特征，选择合适算法，建立模型预测测试集卡号发生电信诈骗的概率。
- **提交结果**：测试集中卡号对应客户借记卡发生电信诈骗的概率，取值范围 0–1，越接近 1 代表发生电信诈骗的概率越大。
- **评测方式**：提交结果按照指定的评价指标进行评测和排名。

---

## 3. 数据说明（Data description）

数据来源于**行内信息**，涉及约 **50 万个客户借记卡卡号**的相关信息，包括交易流水数据和客户、卡号、账号基本信息数据，并对数据进行了欺诈标注，**已做去标识化处理**。

数据分训练数据集和测试数据集两部分：

- 训练集：用于初始建模及模型优化
- 测试集：用于生成欺诈概率结果，分为两份，分别用于初赛和复赛阶段，在比赛不同阶段分别发布

### 3.1 训练数据

共包含 **1 个 ipynb 文件 + 5 个 csv 文件**。

因交易流水数据量较大，训练数据按照训练集及测试集**卡号范围**拆分：

- 初赛部分训练及测试数据以 `_train_testa` 后缀（含 train 训练集及初赛 testa 测试集卡号），在**初赛阶段**发布
- 复赛部分数据以 `_testb` 后缀（含 testb 测试集卡号），在**复赛阶段**发布

| # | 文件 | 内容 |
|---|---|---|
| 1 | `数据字典.ipynb` | 字段说明 |
| 2 | `txn_info_train_testa.csv` / `txn_info_testb.csv` | **交易流水数据**：记录每张卡最新一笔交易往前倒推 **30 个自然日**的交易流水，包含资金流向、交易时间、交易金额、账户余额、交易摘要、交易对手等 |
| 3 | `cst_info_train_testa.csv` / `cst_info_testb.csv` | **客户基本信息**：身份属性、资产规模等 |
| 4 | `card_info_train_testa.csv` / `card_info_testb.csv` | **卡号基本信息**：发卡机构、开卡日期、与客户编号关联关系等 |
| 5 | `accno_info_train_testa.csv` / `accno_info_testb.csv` | **账户基本信息**：开户日期、与卡号关联关系等 |
| 6 | `train.csv` | **训练标注数据**：借记卡是否欺诈，`label=1` 为欺诈，`label=0` 为未欺诈 |

### 3.2 测试数据

测试数据分为 A 榜、B 榜两份数据，分别用于初赛和复赛阶段。

- **A 榜（初赛）**：1 个文件
  - `testa.csv`
- **B 榜（复赛）**：5 个文件
  - `testb.csv`
  - `accno_info_testb.csv`
  - `card_info_testb.csv`
  - `cst_info_testb.csv`
  - `txn_info_testb.csv`

### 3.3 资源优化提示

鉴于交易流水数据规模较大，构建特征工程时可采用**分批迭代处理策略**：每批次特征或样本加工完成后，将中间结果持久化存储至本地并及时释放内存，再进行下一批次处理，以优化内存占用并确保任务稳定运行。

---

## 4. 评分标准（Scoring criteria）

本赛题采用模型在测试数据集的表现进行评分：

1. 以**欺诈概率从高到低排序**
2. 取**前 1%** 对应的概率为**划分点**
3. 大于等于划分点概率值的判为欺诈，否则为非欺诈
4. 计算 **F1-score** 作为最终的评价指标，F1-score 值越高，竞赛排名越靠前

$$
\text{Precision} = \frac{TP}{TP + FP}
$$

$$
\text{Recall} = \frac{TP}{TP + FN}
$$

$$
\text{F1-score} = \frac{2 \times \text{Precision} \times \text{Recall}}{\text{Precision} + \text{Recall}}
$$

> **口径提示**：原文"评价指标"一节表述为"前 1% 的**客户账号**"，"提交说明"一节表述为"前 1% 的**借记卡卡号**"。原文本身存在不一致，但提交文件主键为 `card_no`，实际排序与判定单位应按**卡**处理。

---

## 5. 提交说明（Submission）

### 5.1 提交内容

初赛及复赛环节，需提交针对测试集的预测结果。提交方式为将预测结果存放在 `/personal/作品` 的文件夹下，右键点击【提交结果】。

### 5.2 预测结果的提交格式

- 格式为 **CSV**，文件第一行为字段名，第二行起为具体数据
- 要求**仅包含 `card_no` 和 `score` 两个字段**：
  - `card_no`：借记卡卡号，**数量和顺序必须与测试数据集中的 `card_no` 一致**
  - `score`：预测值，可以为概率值，即借记卡发生电信诈骗的概率
- 最终结果评价会以 `score` 列从高到低排序，取前 1% 的借记卡卡号对应的概率为划分点，大于等于划分点概率值的借记卡为欺诈，否则为非欺诈

```csv
card_no,score
6xxxxxxxxxxxxxx1,0.9731
6xxxxxxxxxxxxxx2,0.0042
```

### 5.3 提交频率

初赛及复赛环节，每位选手**每天均可提交 3 次**预测结果，当日未使用次数**不累计**至次日。

### 5.4 查看结果

- 每次提交后，系统将依据评分规则自动测评并生成成绩，实时计算该环节历史最佳成绩进行排名
- 选手可在 `/personal/作品` 文件夹下，右键点击【查看结果】，进入比赛评分排名页面查看提交历史、最好成绩
- 可点击页面的"查看排行榜"按钮，进入创新服务云平台的赛题排行榜页面查看排名
- 如多位选手成绩相同，则排行榜中**排名并列**

---

## 6. 关键要点提示

> 以下为基于赛题原文的解读与建模建议，**非赛题原文内容**。

### 6.1 由原文可直接确认的

- **阈值是按比例卡前 1%**，而非固定概率阈值 → 模型优化核心是**排序能力**（头部 1% 的命中率），而非概率校准。单调变换（如 rank 归一化、多模型 rank 平均）不改变得分。
- 线下验证应**直接复现"取 top 1% 算 F1"的口径**；AUC / KS 仅作辅助参考。
- 交易流水窗口为**每张卡最新一笔交易往前倒推 30 个自然日**。注意锚点是**每张卡各自的末笔交易时间**，不是统一的日历日：
  - 所有时序特征应按**相对天数**构造（距该卡末笔交易 T-1 / T-3 / T-7 / T-30 的金额、笔数、对手数、余额波动等）
  - 绝对日期只能作为辅助特征（如星期几、是否月末），否则 train / testa / testb 之间会产生时间错位
- 数据表通过 **客户编号 – 卡号 – 账号** 三级关联关系连接，需注意主键与关联口径（一客多卡、一卡多账号的展开方式会影响聚合结果）。
- **提交对齐是硬约束**：`card_no` 数量与顺序必须与测试集一致，最后一步务必用测试集主键做 left join 回填，而非直接输出模型排序结果。
- **每日仅 3 次提交**，A 榜试错成本高 → 必须先把线下验证口径调到与线上一致，再用提交次数验证关键假设。

### 6.2 待数据验证的推断

**正样本比例可能在 1% 量级（推断，赛题未给出）。**

设实际欺诈率为 $r$，评分固定取 top 1% 判为正，则 $TP \le \min(0.01, r) \cdot N$，代入可得 F1 的理论上界：

$$
\text{F1}_{\max} = \frac{2 \cdot \min(0.01,\ r)}{0.01 + r}
$$

该函数在 $r = 0.01$ 处唯一取到最大值 1，向两侧均快速衰减：

| 实际欺诈率 $r$ | 0.5% | 1% | 2% | 5% | 10% |
|---|---|---|---|---|---|
| F1 上界 | 0.667 | 1.000 | 0.667 | 0.333 | 0.182 |

若真实欺诈率明显偏离 1%，无论模型多强，全场选手的分数都会被压在同一个低上界附近，赛题失去区分度。因此主办方大概率是**贴着真实欺诈率**设定的这个比例。

- **验证方式**：拿到数据后对 `train.csv` 的 `label` 做 `value_counts(normalize=True)` 直接确认。
- 若确认接近 1%：属于极度不平衡场景，可考虑类权重、采样策略或排序类损失，但注意采样会破坏概率校准（本赛题不影响得分，因为只看排序）。
- 若实际正样本比例明显高于 1%：说明"前 1%"是刻意收紧的**高精度考核口径**，建模重心应转向优化头部排序的精确率（Precision@1%），而非单纯处理不平衡。
