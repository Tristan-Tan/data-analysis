# B 榜（复赛）优化过程记录

> 本文记录 testB 适配与优化的完整过程：修复了哪些 bug、排除了哪些假设、
> 否定了哪些方向、得到了哪些可复用的结论。所有数字均为内网实测。

---

## 一、成绩时间线

| # | 方案 | 线上得分 | 命中 |
|---|---|---|---|
| — | A 榜最终（testA） | 0.916446 | 691/754 |
| — | A 榜复现重跑（testA） | 0.917771 | 692/754 |
| 1 | 三族融合（402 维），含组内分位混合池 bug | 0.87931 | **663**/754 |
| 2 | 组内分位分批次修复后 | 0.87798 | 662/754 |
| 3 | `+both` 五族融合（追加 nosil partial + full） | 0.87931 | **663**/754 |

- 第一名约 **678/754（≈0.899）**，与我们相差 15 张
- 三次提交锁死在 662~663，**所有改动均在噪声内**
- 注意第一名也显著低于我们 A 榜自己的 691/754 —— testB 全场都更难

---

## 二、修复的三个真实 bug

### 1. `01_parse_raw.py` 完全没有读取 testB 数据的代码

testA 阶段只给一个 `testa.csv`（卡号列表），交易流水已合并在
`txn_info_train_testa.csv` 里；而 B 榜给的是**5 个独立文件**
（`testb.csv` + `txn/card/cst/accno_info_testb.csv`），流水**不在**原文件中。

**修复**：testB 文件存在时自动并入同一份 `txn/card/cst/accno.parquet`。

必须并入同一份而非单独计算——`cp_degree`（对手网络度）、`exp5` 暴露编码
这类**跨卡图特征**依赖"池子里还有哪些卡"，分开算会重现 A 榜踩过的
"参考集口径不一致"问题。

### 2. silence 锚点跨批次错位

testB 末笔日期整体比 train/testA 晚约一个月（testB 2025-12-20~2026-01-31，
train/testA 集中在 2025-12~2026-01-01），是两批独立快照的固有差异。

若按审查材料原计划"统一锚点、同步重算训练侧"，实测后果：

| | train label0 sil_days 均值 | sil≥30 占比 |
|---|---|---|
| 正确（A 榜原锚点） | 7.92 | 1.78% |
| 统一锚点（错误） | **37.92** | **100%** |

silence 族（13.8% gain）将完全失去区分度。

**修复**：train+testA 与 testB **各自用批次内部的末笔日期最大值定锚**
（ANCHOR_A=2026-01-01，ANCHOR_B=2026-01-31）。修复后 train 侧数值与 A 榜
完全一致（label0 7.9161、label1 16.606，对上文档记录的 7.92/16.61）。

> 审查材料原文"绝不可让两侧各自取自己的最大值"这条判断，在批次间隔达到
> **月级**时不成立，已被实测推翻。原计划只适用于几天量级的漂移。

### 3. `cst_id` 跨批次重复导致训练矩阵行数膨胀

同一客户可能在 train/testA 已有卡、在 testB 又开新卡，于是该 `cst_id` 在
合并后的 `cst_info` 里出现两次。`02_features_v1.py` 用 `cst_id` 做
`card.join(cst)`，主键不唯一 → 对应 train 卡被"炸"成多行 → 训练矩阵
300000 变 300016，与 label 长度对不齐（`IndexError`）。

**修复**：`card`/`cst`/`accno` 三张表合并后按各自主键去重（实测丢弃 19 条
重复 `cst_id`）；并在 `assemble.build()` 增加行数校验断言，使同类问题
今后立即报清晰错误而非在下游冒出难懂的 pandas 报错。

---

## 三、排除的五个假设

| 假设 | 证据 | 结论 |
|---|---|---|
| 硬规则 `sil_days≥32` 失效 | testB 仅命中 **1 张**（train 8/300000=2.7e-5，testB 1/75400=1.3e-5，同量级） | 最多影响 1 个名次，排除 |
| `smy_te` 特征拖累 | 两侧分布几乎重合（mean 0.027245 vs 0.026651，std 0.014290 vs 0.014353）；摘要码覆盖率 **99.9976%**；对抗 gain 占比 0.03% | 排除 |
| `exp5` 标签密度失配 | `aa_nf_max` 两侧 486.0 vs 495.1（**+1.9%**），远好于 A 榜出问题时的 +24.6% | fold-matched 修正依然有效，排除 |
| **分群配额失配**（模型给某些人群的名额系统性偏少） | ORACLE 上界实测：13 个分组变量里最大增益仅 **+3 张**，多数为 0 或负（详见 §五.3） | 数学上无空间，排除 |
| **被 drop 的码值含未利用信息** | 5 个字段里 4 个候选区 AUC ≈ 0.50~0.53；仅 `occupation` 达 0.5586（详见 §五.4） | 大部分排除，仅 occupation 保留观察 |

---

## 四、否定的五个优化方向

### 1. 屏蔽 silence 族

假设：testB 末笔日期跨度小 17 天（42 天 vs 59 天），`sil_days` 取值范围被
压缩，train 里"很早就停摆"的欺诈卡在 testB 无对应物，该族在 testB 判别力
打折，而模型 13.8% gain 押在上面。

| 变体 | 单族 OOF | 四族融合 OOF | top754 换动 |
|---|---|---|---|
| `full`（22 维全屏蔽） | 2633（比最弱的 cat 2744 还低 111） | −18 | 34 |
| `partial`（13 维，保留 4 个组内分位） | **2755**（超过 cat） | **+3** | **7** |

**两难**：full 够异构但太弱；partial 够强但与现有三族太同质。partial 保留了
`S_sil_pct_in_ntxn_bin`（dart gain 第 4）等 4 个修复后 PSI 仅 0.017/0.006 的
稳定维度，这正是它强的原因，也正是它"太像"的原因。

### 2. 融合结构枚举（12 种，不重训）

| 候选 | OOF 差 | 换动 |
|---|---|---|
| `+partial` | +3 | 7 |
| `partial_x3`（权重 3 倍） | −2 | 17 |
| `+full` | −18 | 34 |
| `full_x2` / `full_x3` | −35 / −43 | 52 / 67 |
| **`+both`（五族）** | **−3** | **30** |

权衡曲线清晰：想要更多换动就必须付 OOF 代价。最优的 `+both` 提交后得 663
（+1，噪声内）。**partial 与 full 互补**——full 单独进来 OOF 掉 18，配上
partial 只掉 3 而换动保住 30。

### 3. 团伙图平滑（testB 内部关联）

动机：`exp5` 用的是"对手关联过多少 **train 的**已知欺诈卡"，而 **testB
这 75400 张卡彼此之间的关联从未被使用**。电诈系团伙作案、常共享中转账号。

关联确实存在：

- 邻居均分 vs 自身分数 Spearman = 0.2658（testB）/ 0.2443（train）
- 邻居均分 top1% 的卡真实欺诈率 **6.60%** vs 全局 1.00%（6.6 倍）

**但 train 侧真标签验证显示平滑是单调负收益**：

| alpha | 0.005 | 0.01 | 0.02 | 0.05 | 0.1 | 0.2 |
|---|---|---|---|---|---|---|
| OOF 较基线 | −58 | −148 | −336 | −818 | −1371 | −1921 |

**原因**：邻居信息 top1% 精度 6.60%，而**模型本身 top1% 精度 92.3%
（2769/3000），是它的 14 倍**。任何权重的混合都是用弱信号污染强信号。

> 教训：相关性存在（Spearman 0.27）不等于有增量信息。必须用真标签验证
> "平滑后是升是降"，而不是只看相关性。

### 4. 域适应 / importance weighting

协变量偏移要求两分布有**重叠支撑集**。

| 特征集 | 对抗 AUC |
|---|---|
| 全量 405 维 | **1.0000** |
| 剔除 18 维（日历量 + sil 绝对量与交互项）后 387 维 | **0.9997** |

全量 1.0 的机制已查明：`sil_days = 锚点 − 末笔日`，两批锚点星期几不同
（train 2026-01-01 周四 / testB 2026-01-31 周六），故
`(weekday + sil_days) mod 7` 在两侧恒为不同常数 —— 100% 精确的判别规则。

但**剔除后仍有 0.9997**，说明可分性不止来自人工规则。剩余高 PSI 维度：

| 维度 | PSI | 性质 |
|---|---|---|
| `*_nf_max` 系列 | 1.6~1.7 | exp5 计数型统计量 |
| `aa_rate_max` / `aop_rate_max` | 0.49 | exp5 比率统计量 |
| `smy2_0507` | — | train 0.09% vs testB **1.12%（12.6 倍）** |

`smy2_0507` 这类摘要码占比差 12.6 倍是**业务层面的真实差异**。

**结论：train 与 testB 在业务特征分布上就是两批结构性不同的数据**，
可分性来自数据本身，剔几个维度消除不掉，域适应在数学上不成立。

---

### 5. 分群阈值 / 分群归一（未实现即被判死）

动机：§六.1 的误差解剖显示配额比（该组被选中数 ÷ 该组真实正例数）在若干人群上
明显偏离 1 —— 沉默 1–2 天组 0.78、卡龄缺失组 0.70、发卡机构缺失组 0.70。
直觉推断"把名额按真实正例数重分配能捞回一批"。

**实现之前先算了上界**：保持每个人群**组内排序不变**，把 top1% 的 3000 个名额
按该组**真实正例数**分配（用真标签，不可实现，纯粹作为上界）。

| 分组变量 | ORACLE 命中 | 基线 | 上界增益 | 组数 |
|---|---|---|---|---|
| 发卡一级机构 | 2775 | 2772 | **+3** | 14 |
| 婚姻 | 2775 | 2772 | **+3** | 10 |
| 收付角色 | 2773 | 2772 | +1 | 6 |
| 对手覆盖 n_cntrprt | 2773 | 2772 | +1 | 5 |
| 活跃度 n_txn / 入金笔数 n_in / 卡龄 | 2772 | 2772 | 0 | 5~6 |
| 性别 / 行业 | 2771 | 2772 | −1 | 3 / 14 |
| 职业 / 年龄 | 2770 | 2772 | −2 | 14 / 5 |
| 沉默天数 | 2769 | 2772 | **−3** | 6 |
| 财富 月日均 AUM | 2766 | 2772 | **−6** | 6 |

**最大上界 +3 张。这条路线数学上就没有空间，一行实现代码都不必写。**

> **为什么配额比偏离 1 却没有空间**——这是本次最反直觉的结论。配额比 < 1 不代表
> "模型少给了名额"，而是反映**该人群的可识别性本来就低**。沉默 1–2 天组正例率
> 仅 0.160%（全局最低），多给它名额换来的是该组内更差的卡；财富分组甚至出现
> −6，说明按真实正例数配额**比模型自己的分配更糟**。模型的跨组名额分配已经接近
> 最优，问题不在组间，在组内。

---

## 五、误差解剖与新路线实验（`code/labB/` 实测）

> 前三次实验（f01 联合事件、f02 资金路径、exp3 表达方式）都是"往 402 维里加列"，
> 单模型 OOF 全为负（−5 / −13 / −7）。第四次不再猜特征，改为先测量旧模型在哪里错。
> 该脚本不训练、不落盘，全部输出为分组聚合量。

### 1. 基线身份确认

重建的三族 rank 等权融合与 `prediction_resultB.csv` 的 top754 **重叠 754/754**，
逐卡完全一致；OOF top1% 命中 2772/3000，与历史记录零偏差。基线身份无歧义。

顺带证实一条此前只是推断的事实：`prediction_resultB_fuse_plus_both_5fam.csv`
与三族版本**重叠仅 724/754（差 30 张）**，而两者线上得分同为 0.87931。
**30 张换动的净收益精确为 0** —— "线上分数相同"确实不能推断"命中同一批卡"。

### 2. 核心发现：190 张三族共同漏报的正例，画像高度一致

| 正例被几个族漏掉 | 张数 | 占比 |
|---|---|---|
| 0 个（全族命中） | 2702 | 90.1% |
| 1 个 | 71 | 2.4% |
| 2 个 | 37 | 1.2% |
| **3 个（共同漏报）** | **190** | **6.3%** |

共同漏报的 190 张里，**169 张（88.9%）排名落在 [3000, 6000)** —— 不是"完全没看见"，
是"卡在门口差一点"。落在 [15000, 60000) 的只有 5 张，60000 之后 0 张。

**行为画像**（中位数）：

| 特征 | 共同漏报 | 全族命中正例 | 全体负例 | 漏报÷命中 |
|---|---|---|---|---|
| `aum_val` | **8,488** | 209 | 2,163 | **40.6×** |
| `mo_da_aum_val` | **11,359** | 601 | 2,404 | **18.9×** |
| `fio_median` | 94,967 s | 22,123 s | 152,978 s | 4.3× |
| `bal_last` | 18,983 | 10,971 | 952 | 1.7× |
| `n_txn` | 6.5 | 38 | 9 | **0.17×** |
| `n_out` | 4 | 23 | 6 | **0.17×** |
| `n_in` | 3 | 11 | 2 | 0.27× |
| `n_cntrprt` | 5 | 18 | 5 | 0.28× |
| `sil_days` | 6 | 18 | 4 | 0.33× |
| `out_ratio` | 0.28 | 0.42 | 0.50 | 0.67× |
| `n_fastout_1h` | **0** | **5** | 0 | 0 |
| `in_sum` | 29,204 | 34,704 | 3,832 | 0.84× |

**人群富集**（倍率 = 共同漏报占比 ÷ 全族命中占比）：沉默 1–2 天 **8.02×**、
入金笔数 Q1 **5.48×**、纯收款 **4.74×**、对手覆盖 Q1 **4.45×**、活跃度 Q1 **3.94×**、
财富 Q5 2.60×、年龄 45–60 1.97×。

综合画像：**高净值、低频、纯收款、钱进来后没有快速转出、卡还没停摆**。
关键是 `in_sum` 中位数 29,204 与全族命中正例的 34,704 基本持平 ——
**不是金额小所以被忽略，是行为形态不同**。

### 3. 为什么旧模型必然漏掉这批卡

模型的三大支柱对它们**同时给出反向信号**：

| 支柱 | 在这批卡上的表现 | 后果 |
|---|---|---|
| 快进快出族 | `n_fastout_1h` 中位数 0（命中正例为 5）；`fio_median` 是命中正例的 4.3 倍 | 整族为 0 或缺失 |
| silence 族（13.8% gain） | `sil_days` 中位数 6 天（命中正例 18 天）；沉默 1–2 天组正例率 0.160%，全局最低 | **主动把它们往下压** |
| 财富归一 5 维 | `in_vs_aum = in_sum/(aum+100)`，这批卡 aum 中位数是命中正例的 40 倍 | 分母大两个数量级，比值被压扁，**又是反向信号** |

### 4. 被 `02_features_v1.py` drop 掉的码值：只有 occupation 有微弱增量

`02_features_v1.py:129-131` 把 `occupation` / `industry` / `gender` /
`marital_status` / 两级发卡机构的**码值本身全部 drop**，只保留 `{字段}_freq`
频次编码。模型不知道一个人是无业还是学生，只知道这个职业码常不常见。

用 FOLD_SEED=42 的 5 折 OOF 目标编码（训练侧无泄露）测其条件增量。
**判据是候选区 AUC 而非全局 AUC** —— 全局 AUC 高但候选区 ≈ 0.5，说明该信息
只能区分头部易分样本，早已被模型学走。候选区 [3000,6000) n=3000 正例=207。

| 字段 | train 取值数 | testB 非空率 | 全局 AUC | **候选区 [3000,6000)** | 与自身 freq 的 Spearman | 与融合分 Spearman |
|---|---|---|---|---|---|---|
| `occupation` | 506 | 98.17% | 0.6190 | **0.5586** | **−0.1531** | **0.0669** |
| `industry` | 239 | **17.46%** | 0.5264 | 0.5454 | 0.3416 | 0.0181 |
| `crdisu_lvl1_insid` | 38 | 98.41% | 0.5551 | 0.5256 | 0.0971 | 0.0251 |
| `gender` | 3 | 98.21% | 0.5717 | 0.5209 | **0.8682** | 0.0716 |
| `marital_status` | 10 | 50.12% | 0.5438 | **0.5036** | −0.5185 | 0.0308 |

- **`marital_status` 候选区 0.5036 = 纯噪声**；`gender` 与自身 freq 的 Spearman
  0.8682，说明频次编码已经把它表达完了；`crdisu_lvl1_insid` 候选区 0.5256 接近噪声；
  `industry` 候选区 0.5454 但 testB 非空率仅 17.46%，覆盖太低。**四个字段判死。**
- **`occupation` 是唯一例外**：候选区 AUC 0.5586，弱，但与现有 `occupation_freq`
  的 Spearman 仅 −0.1531、与融合分仅 0.0669 —— 是目前测过的所有候选里
  **与现有信息重叠最低的一个**（上一轮 smy_te 的最高相关是 0.4767）。
  跨批次稳定：testB 非空 98.17%、未见过的码 0.00%、各码占比倍率 0.89~1.26。
  单码 lift 跨度大：40207 为 3.32（n=211）、40302 为 3.00（n=1669），
  而 20508 为 0.10（n=1026）、80100/40800/20609 为 0.00。

### 5. 候选池召回上限：问题不在召回，在头部排序

| 候选池 | 覆盖正例 |
|---|---|
| Top2%（6000 张） | 2979/3000 = **99.30%** |
| Top3%（9000 张） | 2986/3000 = 99.53% |
| Top5%（15000 张） | 2995/3000 = 99.83% |
| Top10%（30000 张） | 2997/3000 = 99.90% |

只有 21 张正例掉在 Top2% 之外。**扩候选池没有意义，问题 100% 在头部排序。**

### 6. testB 头部构成与 train 头部的差异

倍率 = testB 头部占比 ÷ train 头部占比（**全体占比的差异是批次固有差异，
真正要看的是头部**）：

| 维度 | 倍率 |
|---|---|
| 入金笔数 Q1 | **1.31** |
| 对手覆盖 Q1 | **1.26** |
| 活跃度 Q1 | **1.25** |
| 纯收款 | 1.13 |
| 沉默 30+ | **0.51** |

testB 头部里低频 / 低对手覆盖的卡比 train 头部多 25–31%，长沉默卡少一半。
**§五.2 那个在 train 上损失 190 张的缺陷，在 testB 上的代价只会更大。**
这与 OOF 92.4% 而线上 87.9%（gap 4.6pt）的方向一致。

### 7. 附带发现：`sil_days = 0` 的跨批次断层

`sil_days = 0` 在 **testB 全体占 25.46%**，而 train 全体为 **0.00%**（仅 5 张）。
testB 有约 1.9 万张卡"活到批次快照最后一天"，train 里这类卡几乎不存在。
该组在 testB 头部只占 1.19%（9 张），未造成直接损失，但属于两批数据的结构性差异，
记录备查。

---

### 8. 低活跃人群专用模型（r01）—— 四条预注册判据全部触发，作废

由 §五.2 的误差解剖直接导出，是第一条不靠"加特征"的路线。
划分由 AUTO 按"正例数达标里组内上界最大"选出：

| 划分 | 定义 | train n | pos | 正例率 | 占名额 | 命中 | **组内FP(名义上界)** |
|---|---|---|---|---|---|---|---|
| A | `n_txn <= 6` | 125,108 | 553 | 0.442% | 526 | 436 | +90 |
| **B（选中）** | `n_in <= 3` | 183,632 | 706 | 0.384% | 684 | 575 | **+109** |
| C | `n_fastout_1h==0 且 n_txn<=15` | 132,282 | 549 | 0.415% | 512 | 419 | +93 |

**结果**：

| 判据 | 预注册阈值 | 实测 | 触发 |
|---|---|---|---|
| 组内 top-m 命中 | 须高于基线 | 575 → **551（−24）** | ✗ |
| 全局换入/换出正例 | 换入须 > 换出 | 11 / 35 → **净 −24** | ✗ |
| 逐折方向 | 须一致为正 | [−1, −4, −11, −8, −1] | ✗ |
| **机构留出方向** | 须与段二一致为正 | [−1, −2, −1, −13, 0]，**方向一致 0/5** | ✗ |

全局 OOF 2772 → 2748。testB 侧换动 13 张，换入卡基线原排名 p50=781。

### 9. r01 失败的机制分析（比结果本身更有价值）

**（1）专用模型确实重新分配了权重，但排序反而更差。**

| 特征族 | 全局模型 gain | 专用模型 gain |
|---|---|---|
| 财富归一（5 列） | — | **23.06%**（`bal_vs_aum` 单列 19.45%） |
| 快进快出/末段（31 列） | — | 25.08%（`h1_in_sum` 单列 11.30%） |
| **silence 族（22 列）** | **13.8%** | **8.46%** |
| exp5 暴露编码（56 列） | — | 6.93% |

机制假设是对的——子人群里有效维度确实不同，专用模型把 silence 权重从 13.8%
压到 8.46%，把财富归一提到 23.06%。**但重分配之后排序更差**，说明
"全局模型的权重被活跃度 Q5 的 1310 个正例主导、对低活跃人群不合适"
这个假设**不成立**：全局模型的权重分配对这个子人群本来就是合适的。

**（2）我用了一个太松的上界——这是本次设计上的真实缺陷。**

我把"组内 FP 数"当作上界（B 划分 +109）。但该数值假设**组内排序可以做到完美**。
实际上子人群内 AUC 已达 **0.9988**，基线组内头部精度 575/706 = **81.4%**；
要吃满 +109 需要达到 96.9%。从 81.4% 到 96.9% 不是"重新校准阈值"能做到的，
剩下的 18.6% 大概率是**信息不足**而非**校准不当**。

> **正确的上界应该估计"组内可达上界"，而不是"组内 FP 数"。**
> 组内 FP 数只是"如果排序完美"的算术差值，对可行性零约束。
> §四.5 的 ORACLE 上界之所以有效，是因为它固定了组内排序、只放开配额，
> 放开的那个自由度是真实可控的；而组内排序不是。

**（3）新找回的正例全部挤在极窄的一条带里。**

11 个新找回正例的基线原排名：min=3002、**p50=3040**、max=3410。
专用模型只能把**刚好差一点**的卡往上挪一点点，挪不动真正被埋的那些。
净 −24 = 挪上来 11 个、挤掉 35 个，是典型的排序质量下降，不是发现新信息。

**（4）机构留出 fold3：AUC 上升而 top-k 命中下降 13。**

`AUC 0.9806 → 0.9823`（上升），`名额 376 内命中 113 → 100`（下降 13）。
**在 1% 正例率下，AUC 改善与 top-k 改善可以背离**。任何用 AUC 作为
中间指标的判断都必须再看 top-k，这是本赛题第二次踩到同类陷阱
（第一次是图平滑的 Spearman 0.27）。

**（5）最值得记住的一条：「有一致画像」≠「在该画像人群内部可分」。**

§五.2 的 190 张共同漏报确实画像高度一致（aum 40 倍、n_txn 0.17 倍、
`n_fastout_1h` 恒为 0），诊断没有错。但它们在低活跃人群内部，
**和大量正常的低活跃卡长得一样**——之所以是欺诈，靠的是流水里根本没有的
信息（受害人报案）。**误差解剖能告诉你"错在哪"，不能告诉你"能不能修"。**

> 补充：直接被证伪的是划分 B。A 与 C 未单独重跑，但（2）的机制论证
> 对三者同样成立（三者组内 AUC 与头部精度结构相近），不值得再花 20 分钟。

---

## 六、四个关键洞察

### 1. 高 PSI ≠ 掉分（花一次提交换来的教训）

评分口径是「**testB 内部**排序取前 754」，只看这 75400 张卡**彼此之间**的
相对次序。组内分位 bug 的性质是：修复前 testB 卡在混合池排名、分位值被压
在 0.41 均值，修复后铺满 0~1 到 0.50 —— 但**同一分箱内 testB 卡之间的先后
顺序两种算法下一致**，这基本是个**保序变换**。

保序变换不改变 top754 是哪些卡 → 不改变分数。实测 663→662（−1，噪声内），
与推断完全吻合。

> **只有改变 testB 内部相对排序质量的因素才影响分数；单纯的全局尺度偏移，
> 无论 PSI 多高，都基本无害。** 这条推翻了"按 PSI 排风险分去定点修复"的
> 整条路线。

### 2. 边界密度 0.027% —— 排序在边界处接近掷硬币

| 名次区间 | 分数跨度占全量极差 |
|---|---|
| 744~764 | **0.027%** |
| 724~784 | 0.080% |
| 704~804 | 0.133% |
| 654~854 | 0.265% |

top754 附近的卡分数几乎完全相同。这解释了：

- 三次提交为何锁死在 662/663 之间横跳
- 图平滑 alpha=0.1 为何能洗掉 65% 的 top754（必须细到 0.005 量级）
- 为什么多 seed 降方差在本题比一般情况更有价值 —— **单 seed 的随机性
  （bagging/feature_fraction 抽样）实际上主导了谁进 top754**

### 3. 模型本身是稳的，不是概念漂移导致的崩溃

三族在 testB 头部的判断高度一致：

| 族对 | train top1% 重叠 | testB top1% 重叠 | 差值 |
|---|---|---|---|
| dart vs abthin_lgbn | 0.9783 | 0.9748 | −0.0035 |
| dart vs cat | 0.9667 | 0.9496 | −0.0171 |
| abthin_lgbn vs cat | 0.9650 | 0.9562 | −0.0088 |

差值仅 0.4%~1.7%，远低于"模型整体不稳"的判据（0.1）。

### 4. 训练侧 OOF 与线上已完全脱钩

| | 线下 OOF | 线上 |
|---|---|---|
| A 榜 | 2775/3000 = 92.5% | 691/754 = 91.6%（gap 0.9pt） |
| B 榜 | 2772/3000 = 92.4% | 663/754 = 87.9%（gap **4.6pt**） |

OOF 几乎一样而线上差 28 张 —— **OOF 对跨批次迁移损失完全不敏感**，
不能用于方案选择。这是 B 榜与 A 榜最本质的差别。

---

## 七、可复用的诊断方法论

1. **对抗验证**：train 标 0 / testB 标 1 训分类器，AUC 衡量分布差异，
   feature importance 定位漂移源。注意联合模型会被组合规则主导
   （本例 `(weekday+sil_days) mod 7` 吃掉 86% gain），需配合逐特征诊断。
2. **PSI × gain 风险分**：逐特征算 PSI 再乘以模型 gain 占比，排出
   "既重要又漂移"的维度。⚠ 但须配合洞察 1 —— 高 PSI 未必掉分。
3. **三族一致性对比**：同一次训练的 train OOF 与 test 两侧，比较两两
   Spearman 与 top1% 重叠度，区分"个别特征漂移"与"模型整体不稳"。
4. **真标签验证**：任何后处理（如图平滑）先在 train 的真标签上验证效果
   方向，再决定是否用在测试集上。成本几分钟，能省下宝贵的提交次数。
5. **换动张数作为信息量指标**：提交前先算候选与已提交版本的 topK 差异，
   <20 张则结果无法归因（涨跌都分不清是方向对还是噪声），等于白花一次
   提交次数。

---

6. **先算上界，再决定要不要实现**。任何"重新分配 / 重新加权 / 后处理"类
   想法，都可以用真标签构造一个 ORACLE 上界（例如分群配额：保持组内排序不变，
   按真实正例数分配名额）。该上界用了标签、不可实现，但**上界小就等于路线死**。
   §四.5 用这招在写实现代码之前就判死了分群重标定，成本几十秒。
7. **判断一个新特征有没有用，看候选区 AUC，不看全局 AUC**。全局 AUC 高而候选区
   AUC ≈ 0.5，说明该信息只能区分头部易分样本，早被模型学走。§五.4 用这招把
   5 个候选字段筛到只剩 1 个。前三次实验栽的正是这个跟头：全局指标都不难看，
   一进边界区就归零。

---

## 八、当前状态

### 六个方向已全部关闭，每个都有实测证据

| 方向 | 判死证据 |
|---|---|
| 扩候选池 / 提召回 | Top2% 已覆盖 99.30% 正例，只有 21 张掉在外面 |
| 分群配额重标定 | ORACLE 上界最大 +3 张，沉默分组 −3、财富分组 −6 |
| **分群组内排序（r01）** | **组内 −24；机构留出方向一致 0/5** |
| 加特征（f01/f02/exp3 三次） | 单模型 OOF −5 / −13 / −7 |
| 被 drop 的码值（5 个字段） | 4 个候选区 AUC ≈ 0.50~0.53；`occupation` 0.5586，太弱 |
| 图平滑 / 域适应 / 融合结构 / 屏蔽 silence | 见 §四.1~4 |

### 加上一条横贯全局的硬约束

**OOF 与线上脱钩**：A 榜 gap 0.9pt，B 榜 gap **4.6pt**（§六.4）。
即使某方案 OOF 涨几张，也无法判断线上是涨是跌。在这个条件下动用提交次数
是在赌，不是在优化。

### 结论

15 张的差距来自 train→testB 的结构性迁移障碍。这不是"还没想到好点子"，
而是**搜索空间已被系统性排除**：召回侧、组间配额、组内排序、特征增量、
静态属性、后处理平滑、域适应——七条路各自有独立的实测反证。

**建议：不动用任何提交次数，663/754 定稿。**

### 唯一的未决项

`occupation`（§五.4）候选区 AUC 0.5586，与现有信息重叠最低（与自身 freq
仅 −0.1531、与融合分仅 0.0669）。但 0.5586 这个量级单独成路线不够格，
而"加进 402 维"正是已经失败三次的模式。r01 失败后也失去了"作为子人群模型
输入"的载体。**除非有新的证据，否则不做。**
