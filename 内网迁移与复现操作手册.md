# 内网迁移与复现操作手册

> 面向**执行迁移的人**（不是评审）。目标：把当前内网这套赛时的目录，
> 改造成两榜都能"从落盘模型直接预测"的交付包。
>
> 总耗时约 **3 小时**（B 榜 25 分钟 + A 榜 2.6 小时），比两榜全量重跑省 2 小时。
>
> **执行顺序不能颠倒**：先做 B 榜。B 榜快，而且能用当初实际拿到 664 的那份
> 文件反证整条链路是对的；万一有问题，不至于先白跑 A 榜 2.6 小时。

---

## 第 0 步：确认现状与备份（5 分钟，别跳过）

```bash
cd <项目根>

# 0.1 看清楚现在有什么
df -h .                                   # 剩余磁盘，需要 ≥ 15GB
du -sh data/interim model prediction_result 2>/dev/null
ls prediction_result/ | wc -l             # 赛时候选 csv 有 20+ 个
```

```bash
# 0.2 备份。后面有覆盖动作，这一步是唯一的后悔药
mkdir -p _backup
cp -a model                _backup/model_赛时B
cp -a prediction_result    _backup/prediction_result_全部候选

# 0.3 nosil 的 npy 稍后会被 11 号脚本覆盖，单独留一份用于事后核对
cp data/interim/oof_nosil*.npy data/interim/te_nosil*.npy _backup/ 2>/dev/null
ls _backup/*nosil*
```

> ⚠ **full 变体的文件名没有后缀**。早期那版 `no_silence_variant.py` 存的是
> `te_nosil.npy` / `oof_nosil.npy`（不带 mode），那次跑的正是 **full** 模式；
> partial 是后来跑的，才有 `_partial` 后缀。生成 664 的那次融合，full 成员
> 读的就是这个无后缀文件（赛时的融合脚本对两种命名都做了兼容）。
> 所以这里预期能看到四个文件：`te_nosil.npy`、`oof_nosil.npy`（= full）
> 与 `te_nosil_partial.npy`、`oof_nosil_partial.npy`。
>
> 这不影响正式链路 —— `11_train_nosil.py` 是**重新训练**并写出规范命名的
> `te_nosil_{partial,full}.npy`，不读旧文件。旧的无后缀文件核对完即可删。

> `_backup/` **不打包进交付材料**，全部跑通后可删。

---

## 第 1 步：目录改造（1 分钟）

赛时是单目录，新版按赛段隔离。三条 `mv` 即可，**不重算任何东西**：

```bash
# 1.1 赛时的 data/interim 装的就是 B 榜产物，直接改名复用
mv data/interim data/interim_B

# 1.2 赛时的 model/ 装的就是 B 榜三族模型，移入 model/B/
mkdir -p model/A model/B
mv model/*.txt model/*.cbm model/*.json model/B/

# 1.3 确认
ls model/B/ | wc -l          # 应为 22（20 个模型 + 2 个列顺序 json）
ls data/interim_B/*.parquet | wc -l
```

然后把新版代码覆盖进 `code/`，并确认这几个新文件到位：

```bash
ls code/train/11_train_nosil.py code/selftest/ code/scripts/run_features.sh
```

> **为什么赛时的 B 榜产物可以直接复用**：本次改动只涉及 `smy_te` 的启用开关
> 与赛段路径。B 榜 `use_smy_te=True`，与赛时行为完全一致，中间产物与模型
> 逐字节有效。第 2 步里有一道自动校验会当场证实这一点。

---

## 第 2 步：B 榜（约 25 分钟）

```bash
export FRAUD_ROUND=B
export PYTHONPATH=code
```

### 2.1 先把旧的同名提交文件改名让位

`prediction_resultB.csv` 现在是 663 的三族版，而最终成绩 664 来自
`prediction_resultB_fuse_partial_x2plus_full.csv`。预测会写同名文件，先改名：

```bash
mv prediction_result/prediction_resultB.csv \
   prediction_result/prediction_resultB_3fam_663.csv
```

### 2.2 补训 nosil 两族并落盘（约 20 分钟）

```bash
python code/train/11_train_nosil.py
```

**开头必须看到这一行**：

```
  ✔ 基础列与已落盘的三族模型一致（405 维）
```

看到它，就证明复用赛时三族模型是安全的。若报
`基础特征列与 model/B/feature_cols.json 不一致`，**立刻停下**，把报错贴给我
——那说明中间产物与三族模型不是同一批，需要连同第 10 步一起重跑。

跑完检查：

```bash
ls model/B/nosil_*                # 应有 10 个 .txt + 2 个 json
```

### 2.3 生成正式提交文件（约 5 分钟）

```bash
bash code/scripts/run_predict.sh B
```

输出为 `prediction_result/prediction_resultB.csv`（6 成员配方 + 硬规则）。

### 2.4 用实际拿到 664 的文件反证（关键）

```bash
python code/selftest/compare_submission.py \
    prediction_result/prediction_resultB_fuse_partial_x2plus_full.csv
```

**看这一行**：

```
top754 集合重叠: 754/754   差异 0 张
```

- **754/754** → 新链路与当初逐环节等价，B 榜复现**得到实证**。这比任何自检都硬。
- **差几张** → 脚本会打印差异卡落在哪个排名带。若都紧贴 754 边界，属
  `best_iteration` 浮点抖动；若散布在很靠后的排名，说明链路有实质差异，
  把输出贴给我。

顺手核对 nosil 是否与赛时一致（可选）：

```bash
python - <<'EOF'
import numpy as np, os
# full 的赛时产物是无后缀命名（见第 0.3 步说明）
pairs = [("partial", "_backup/te_nosil_partial.npy"),
         ("full",    "_backup/te_nosil.npy")]
for mode, old in pairs:
    new = f"data/interim_B/te_nosil_{mode}.npy"
    if not os.path.exists(old):
        print(f"{mode}: 无赛时参照 {old}，跳过")
        continue
    a, b = np.load(old), np.load(new)
    print(f"{mode}: " + ("完全一致 ✔" if np.array_equal(a, b)
                         else f"有差异，最大 {np.abs(a - b).max():.2e}"))
EOF
```

两族当初的超参、折划分、seed 与新脚本完全相同（`LGB_BASE + LGBN_EXTRA`、
`StratifiedKFold(5, random_state=42)`、seed 42），剔除的列也是同一份清单，
**预期完全一致**。若不一致，说明中间产物在那之后被动过，停下来查。
这条只是辅助，真正的裁判是上面的 `754/754`。

### 2.5 交付包静态自检

```bash
python code/selftest/verify_package.py
```

应输出 `自检全部通过`。

---

## 第 3 步：A 榜（约 2.6 小时）

A 榜必须**从零重跑**：赛时模型已被 B 榜覆盖，且必须在**不含 testB 的参考池**
下重算特征（详见审查材料 §二）。

```bash
# 3.1 现有的 A 榜提交文件先改名保护（预测会写同名文件）
cd prediction_result
for f in prediction_resultA*.csv; do
    [ "$f" = "prediction_resultA.csv" ] && mv "$f" "prediction_resultA_赛时提交.csv"
done
cd ..
ls prediction_result/prediction_resultA*
```

```bash
# 3.2 训练 + 预测
export FRAUD_ROUND=A
bash code/scripts/run_train.sh A        # 约 2.5 小时
bash code/scripts/run_predict.sh A      # 约 5 分钟
python code/selftest/verify_package.py
```

**跑完确认基础维度**：

```bash
python -c "import json;print(len(json.load(open('model/A/feature_cols.json'))))"
```

预期 **401**。若不是 401，说明特征脚本较 A 榜当时有过改动，把实际值告诉我。

**与赛时的 A 榜提交对比**（A 榜有两个历史文件，可分别比）：

```bash
export FRAUD_ROUND=A
for f in prediction_result/prediction_resultA_赛时提交.csv \
         "prediction_result/prediction_resultA_691_754 备份.csv"; do
    [ -f "$f" ] && python code/selftest/compare_submission.py "$f"
done
```

> **这里的差异是预期的，不是错误**。A 榜"从零重跑"本身就有 ±3 卡的浮点浮动
> ——当初原始提交 691、重跑后 692，正是这个机制。两榜均已截止，无法再提交
> 验证，所以 A 榜只能做到"本包内部误差为 0"，做不到"与当初那批模型逐卡相同"。
> 审查材料 §四（七）已如实写明这一点。

---

## 第 4 步：清理与归档

### 保留（交付必需）

| 路径 | 说明 |
|---|---|
| `code/config.py`、`assemble.py`、`account_balance.py` | 全局配置与共用模块 |
| `code/train/`、`code/test/`、`code/scripts/`、`code/selftest/` | 复现链路，**缺一不可** |
| `model/A/`、`model/B/` | **核心交付物**，让评审 3 分钟出结果 |
| `prediction_result/prediction_resultA.csv` | A 榜正式提交 |
| `prediction_result/prediction_resultB.csv` | B 榜正式提交 |
| `data/README.md` | 数据放置说明 |
| `requirements.txt` | 版本锁定 |
| `选手姓名-赛题X-审查材料说明.md` | 审查材料主文档 |
| `B榜优化过程记录.md` | 复赛完整实验记录 |

### 可选（不影响复现，也不影响审查材料的完整性）

`code/explore/` 与 `code/labB/` 是赛中的一次性诊断与被否定方向的实现。
复现链路对它们**零引用**（已实测：删掉这两个目录后两个赛段全链路照常跑通），
审查材料的结论与数字也全部写在正文里，不依赖这两个目录。
打不打包都可以，按评审对材料体积的要求决定。

### 归档（不打包，但内网先留着）

```bash
mkdir -p _archive
# 赛时的 20+ 个候选 csv，只留两个正式的和验证基准
mv prediction_result/prediction_resultB_fuse_*.csv      _archive/ 2>/dev/null
mv prediction_result/prediction_resultB_gs_deg*.csv     _archive/ 2>/dev/null
mv prediction_result/prediction_resultB_nosil4fam*.csv  _archive/ 2>/dev/null
mv prediction_result/prediction_resultB_moreseeds.csv   _archive/ 2>/dev/null
mv prediction_result/prediction_resultB_3fam_663.csv    _archive/ 2>/dev/null
mv prediction_result/prediction_resultA_*.csv           _archive/ 2>/dev/null
ls prediction_result/        # 应只剩 prediction_resultA.csv 与 prediction_resultB.csv
```

> ⚠ `prediction_resultB_fuse_partial_x2plus_full.csv` **等第 2.4 步验证通过后**
> 再归档，它是唯一的验证基准。

### 可以删除

```bash
rm -rf data/interim_labB/       # labB 实验中间产物，不参与复现
rm -rf prediction_result/labB/  # labB 候选预测
rm -rf catboost_info/           # CatBoost 训练日志
rm -rf _backup/                 # 全部跑通后再删
find . -name __pycache__ -type d -exec rm -rf {} + 2>/dev/null
```

### 磁盘紧张时

`data/interim_A/` 与 `data/interim_B/` 各约 8~10GB。A 榜预测完成后可删
`interim_A`（**保留 `model/A/` 与提交文件**）；日后需要再次预测 A 榜时：

```bash
bash code/scripts/run_features.sh A && bash code/scripts/run_predict.sh A
```

约 25 分钟即可重建。

---

## 第 5 步：打包前的最后检查

```bash
# 5.1 两榜都能通过静态自检
FRAUD_ROUND=A PYTHONPATH=code python code/selftest/verify_package.py
FRAUD_ROUND=B PYTHONPATH=code python code/selftest/verify_package.py

# 5.2 看模型体积——这一项可能影响能不能打包
du -sh model/A model/B
```

> **如果 `model/` 总体积过大**（dart 跑满 3000 轮，单个模型可能几十 MB，
> 两榜合计有可能到 GB 级），而评审对材料体积有限制，请把实际数字告诉我，
> 我给压缩方案（LightGBM 文本模型 gzip 压缩率很高，且可在 `predict.py` 里
> 透明解压）。**不要自行删模型来减体积** —— 那会让"落盘直接预测"这条
> 复现路径失效。

### 打包建议

| 打包 | 不打包 |
|---|---|
| `code/`、`model/A|B/`、两个正式提交 csv | `data/train|testA|testB/`（行内数据） |
| 三份 md 文档、`requirements.txt` | `data/interim_A|B/`（可重新生成，体积大） |
| `data/README.md` 与空目录骨架 | `_backup/`、`_archive/`、`catboost_info/` |

> 评审若只拿到 `model/` 而没有中间产物，先跑
> `bash code/scripts/run_features.sh B`（约 25 分钟）补齐特征，
> 再 `bash code/scripts/run_predict.sh B`。审查材料 §四（五）已写明。

---

## 出问题怎么回滚

| 症状 | 处理 |
|---|---|
| 第 2.2 步报"基础特征列不一致" | 停下，把报错贴出来。多半需要连 `10_train_models.py` 一起重跑（B 榜全量约 2.9 小时） |
| 第 2.4 步重叠远小于 754 | 停下，贴出差异卡的排名分布 |
| 任何一步把文件搞乱了 | `rm -rf model prediction_result && cp -a _backup/model_赛时B model && cp -a _backup/prediction_result_全部候选 prediction_result`，回到第 1 步之前 |
| 脚本报 `FRAUD_ROUND 未设置` | 这是刻意设计，不是 bug。`export FRAUD_ROUND=A` 或 `B` |
| DART 打印 `Early stopping is not available in dart mode` | 正常现象，该族固定跑满 3000 轮，见审查材料 §五问题 6 |

---

## 一页速查

```bash
# ===== 准备 =====
mkdir -p _backup && cp -a model _backup/model_赛时B
cp -a prediction_result _backup/prediction_result_全部候选
mv data/interim data/interim_B
mkdir -p model/A model/B && mv model/*.txt model/*.cbm model/*.json model/B/

# ===== B 榜（25 min）=====
export FRAUD_ROUND=B PYTHONPATH=code
mv prediction_result/prediction_resultB.csv prediction_result/prediction_resultB_3fam_663.csv
python code/train/11_train_nosil.py                 # 看到 "✔ 基础列一致" 才继续
bash code/scripts/run_predict.sh B
python code/selftest/compare_submission.py \
    prediction_result/prediction_resultB_fuse_partial_x2plus_full.csv   # 要 754/754
python code/selftest/verify_package.py

# ===== A 榜（2.6 h）=====
export FRAUD_ROUND=A
mv prediction_result/prediction_resultA.csv prediction_result/prediction_resultA_赛时提交.csv
bash code/scripts/run_train.sh A
bash code/scripts/run_predict.sh A
python code/selftest/verify_package.py
python -c "import json;print('A 榜维度', len(json.load(open('model/A/feature_cols.json'))))"
```
